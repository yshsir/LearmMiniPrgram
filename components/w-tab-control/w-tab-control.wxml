<view class='tab-control'>
  <block wx:for='{{titles}}' wx:key='index'>
    <view class="tab-item {{currrenIndex == index? 'active' : ''}}"
          bindtap="onItemClick"
          data-index="{{index}}">
      <text>{{item}}</text>
    </view>
  </block>
</view>